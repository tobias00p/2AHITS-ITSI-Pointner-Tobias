Zu erst müssen wir das file downloden  (pwcheck) das macht man mit `curl -O https://www.franzmatejka.at/htl/doc/ITSI_2_linux/opcode_patching/bin/replit/pwcheck` danach geben wir `ls -l` in shell ein dan gbene wir diesen befehl ein  ` file pwcheck`und danach das wir das file anschauen könne `cat pwcheck`  dann geben wir `xxd pwcheck` um es in einen biener code zu schreiben wir wollen mehr wissen mit dem biener code also geben wir dann noch `xxd pwcheck | more `ein 
mit `./pwcheck` starten wir das programm. wir geben dann noch `strings pwcheck` ein um strings auszugebn dann kann man das program starten man muss dann noch das gdb downloden das macht man mit `gdb --version` downlodet

wir geben im gdb bei --`q` und auch noch im gdb `display/i $pc` danach geben wir `ni` ein  dan jumpen wir das nächste gdb bis zu ``check password`` dan jumpt man bis `` lea `` und giebt in gdb. wir patchen jetzt das program so das der jump ausgeführt wir wir steigen aus dem gdb aus mit `q` und dann steigenn wir mit  `gdb pwcheck `wider ein und geben `run` ein und gleich dannach im gdb noch `disas` fürs disasemblieren ein.  diese zeilen werden ausgegeben

0x00005579c846c1bb <+75>:    lea    0x50(%rsp),%rsi
 0x00005579c846c1c0 <+80>:    call   0x5579c846c2a0 <_Z14check_passwordPKcS0_>
 0x00005579c846c1c5 <+85>:    cmp    $0x0,%eax
 0x00005579c846c1c8 <+88>:    je     0x5579c846c263 <main()+243>
 
 0x00005579c846c1ce <+94>:    lea    0xe55(%rip),%rdi        # 0x5579c846d02a
 0x00005579c846c1d5 <+101>:   mov    $0x0,%al

 je = 6 Byte

 x/16xb 0x401000     # show 16 bytes of data at address 0x401000 in hexdas ist ein gdb befeh also nur in gdb verwendbar 
 
wir steigen jetzt aus dem gedb aus mit `q` und jetzt geben wir diesen befehl ein  ` x/16xb  ` mit der speicheradress von der zeile
dann geben wir x/3i mit einer anderne adresse aus einer zeile ein 




diese 6 zeilen copieren wir in das shell programm ein